<div class="box">
  <h2 class="box-title">ポートフォリオ作成（１）金融機関の選択</h2>
  <div class="box-inner">
    <%= form_for(:portfolio, url: constituent_path) do |f| %>

      <div class="form-group">
        <%= f.collection_select :security_id, Security.all, :security_code, :name %>
      </div>

      <div class="submit-group">
        <%= f.submit "次へ進む", class: "btn btn-success" %>
      </div>
    <% end %>
  </div>
</div>